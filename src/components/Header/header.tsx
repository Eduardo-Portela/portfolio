import { Headers } from "./style";
import { Burger } from "./burger";

export const Header = () => {
  return (
    <Headers id="header">
      <div>
        <h2>Portifolio</h2>
      </div>
      <Burger />
    </Headers>
  );
};
